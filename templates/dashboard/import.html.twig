{% extends "@EasyAdmin/default/layout.html.twig" %}

{% block main %}
    {{ form_start(form) }}
    {{ form_row(form.file) }}
    <br>
    <div class="button-action">
        {{ form_row(form.import) }}
    </div>
    {{ form_end(form) }}
{% endblock %}